#include "main.h"

/*
 * _isdigit - checks for a digit in a group of characters
 * @c - is the integer to be checked
 * return - 1 if c is a digit, otherwise returns 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
